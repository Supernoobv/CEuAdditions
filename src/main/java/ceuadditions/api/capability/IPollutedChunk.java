/* package ceuadditions.api.capability;

import gregtech.api.worldgen.bedrockFluids.ChunkPosDimension;
import net.minecraftforge.common.capabilities.ICapabilityProvider;

public interface IPollutedChunk {
    boolean isPolluted();
    int getPollutionAmount();
    void setPollutionAmount(int amount);
    void tickPollutionInWorld(int tickid);
}


 */